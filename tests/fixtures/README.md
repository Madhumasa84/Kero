# Synthetic test fixtures

Tests generate JPEG and PNG bytes with NumPy and OpenCV in temporary
directories. This directory is reserved for non-sensitive fixture metadata and
documentation. Do not place patient images here or commit patient images to
Git.
